machine:
  xcode:
    version: 7.3.1
  pre:
    - if [ ! -e '$HOME/virtualenvs/venv-system' ]; then mkdir -p '$HOME/virtualenvs'; pushd '$HOME/virtualenvs'; virtualenv venv-system; popd; fi

dependencies:
  pre:
    - make install-deps
    - pyenv local 2.7.10 3.5.0

test:
  override:
    - make ci